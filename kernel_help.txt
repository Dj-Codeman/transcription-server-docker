I need help designing a specific component in a
system that I'm overhauling, what I did was take the manager application that I
had originally for the runners which did both all of the application lifetime
management the upstream kernel driver communication the EBPF monitoring for
along with the lifecycle work and then it also had the network stack to
communicate upstream with all of the additional like API's and like
observation platform stuff and I separated it out so the manager now is
really just a shell where it just handles all of the upstream network
registration orchestration and getting like command and control stuff from like
the network and then I made a separate application which is the watchdog that
does all of the application lifecycle management it has the kernel driver
communication and it does the EBPS stuff and it manages the client versus system
applications so I also got the kernel driver implemented properly so if the
user land watchdog application is killed the system resets and reinitializes
everything the only thing is I need some way because previously the manager if it
restarted for any reason that would all be logged with the manager and that data
could be pulled upstream to whatever like the visibility platform was now
that doesn't necessarily exist because the way the kernel driver resets the
system is with like an emergency reset like syscall so there's not really a
lot of time pre-execution of that to like write data out so in the kernel
driver I have a section that's called a user land helper function and the
intention of that is right before the systems reset it has like maybe a second
or two to maybe like write some data out store some files or just do some like
some last-second like wrap-ups before the systems reinitialized what I want to
do is figure out some way to make the manager aware of that outside of just
like straight up logging it and like parsing through like specific log
entries so like I want like a flag I was thinking initially I can have the socket
on the manager application and when the kernel app reset it would send a signal
to that flag and that manager would write a small like hey this is what
happened this is why we're resetting whoop-de-whoop but that's a user land
application a kernel driver then that kernel driver will also need to
communicate with an other application and that seems a bit complicated so I'm
just trying to figure out how from the kernel drivers perspective because I
also want to limit the amount of user land interacting that the kernel driver
does I don't want it to just be doing stuff freely in the user land because
that's problematic I need to figure out a way to store like a snapshot or like
an incident log or like a crash report or something that the watchdog can see
sure but really that the manager can pick up and report upstream and I just
need an approach on how to do that because I'm working with C and Rust and
if I'm doing like sockets or like IPC calls I'm not the best at doing that NC
so like I'd really rather send like the shortest structure or the shortest type
of object possible from the kernel and then just let like the rust code and
like the user space clean it up and write it out but like that would also
need like some kind of hook or some kind of delay in the kernel driver because if
all it's in the user space is just like a one-shot type send that data off to the
user land then it'll mark that function as complete and then move to the reset
before we necessarily guarantee that the user land applications have had time to
process and store it but at the same time I don't want to make the reset
dependent on the user space application confirming that it did because there is
a case that like the manager is the issue or something or another that would
prevent the reset functionality from triggering because it comes from an
issue in the user space so it's like a bit of a catch-22 with it
